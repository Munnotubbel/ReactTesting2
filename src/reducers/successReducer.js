/**
 * @function - successReducer
 * @param {array} state - Array of guessed words.
 * @param {onject} action - action to be reduced.
 * @returns {boolean} - new success state.
 */

export default (state, action) =>{
    return null;
}